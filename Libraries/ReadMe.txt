This folder contains the C++ libraries that are needed to build the products.

JUCE: Jules' Utility Class Extensionsxtensions (cross-platform framework with focus on audio)
RAPT: Rob's Audio Processing Templates (template-based audio DSP toolkit)
rosic: Rob's Signal Processing Classes (non templated legacy DSP library)

todo:
-maybe put the GNUPlotCPP library here as well - remove it from the RAPT folder
-propagate rosic classes up to RAPT templates and let the corresponding class the become and 
 instatiation of the template ...one by one