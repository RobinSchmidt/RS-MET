  
Add a new type of modulator
===========================

- Opcode enum: Add opcodes corresponding to the modulator's parameters

- OpcodeType enum: Add an appropriate entry, probably after FreeEnv, FreeLfo, etc.

- SfzCodeBook class: (1) Add the appropriate lines for just created opcodes in contsructor, e.g. a 
  line like: add(OC::my_new_opcode, ...). (2) Add a line to modSourceToString, stringToModSource.
  
- Add an appropriate subclass of Processor and implement its constructor, processFrame and
  updateCoeffs member functions.
  
- DspResourcePool class: (1) add an array of objects of the new type, (2) add new lines to 
  allocateModulators, grabModulator, repositModulator.