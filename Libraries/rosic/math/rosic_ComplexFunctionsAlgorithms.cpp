#include "rosic_Complex.h"
using namespace rosic;

/* This cpp file contains some functions which carry out the internal algorithms which a required
in the evaluation of certain special functions for complex numbers. */
   




void landen(double k, int M, Complex* v)
{
  // performs M iterations of the Landen transformation of an elliptic modulus k and returns the 
  // results in the array v which must be of length M+1

}
