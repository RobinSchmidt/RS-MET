#ifndef RS_RSBUILDCONFIG_H
#define RS_RSBUILDCONFIG_H

// Copy this file to some place in your project directory tree and adjust the directives as
// desired. Then, add the directory containing your copy to the compiler's include paths.

#define RS_BUILD_DLL 0  // enable this when building the dll
#define RS_USE_DLL 0    // enable this in a project that uses the RSLib dll

#define RS_BUILD_DLL 0
#define RS_USE_DLL 0

#endif
