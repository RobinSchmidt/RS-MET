
- Make allpass unit tests pass again by givin rsSparseRationalFunction a tolerance and then using 
  it in the unit tests. In dampedMultiCombAllpassUnitTest(), we need to change 
    using TransFunc = rsSparseDigitalTransferFunction<Real>;
  to
    using TransFunc = rsSparseDigitalTransferFunction<Real, Real>;
  such that we don't get te default value of rsEmptyType for the tolerance type. But first, we 
  actually have to add the 2nd template parameter TTol to rsSparseDigitalTransferFunction. We may 
  need to give classes like rsDampedCombAllpass a way to determine the tolerance type. Maybe let 
  the getCombTransferFunctionAt() etc. functions take a tolerance as parameter. 


- Clean up rsSparseRationalFunction


- figure out, why rsSinusoidalAnalyzer may produce negative values for the amplitude of the DC 
  component (and fix it)

- check, how the synthesis handles DC - i guess it creates just a cosine of 0 frequency? if so, we 
  should perhaps encode a negative DC by using a phase of pi

- check rsFourierTransformerRadix2<T>::getRealSignalMagnitudesAndPhases - there seems to be a bug