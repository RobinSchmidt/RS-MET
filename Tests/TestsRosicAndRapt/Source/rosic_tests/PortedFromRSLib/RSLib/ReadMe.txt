/**
This directory contains a few RSLib classes that should not go into rapt or rosic but are 
required by some of the test/example functions. Eventually, these test functions should be 
updated so we can get rid of these files here in the test project.
*/
