#ifndef RS_MATRIXTESTS_H
#define RS_MATRIXTESTS_H

bool testMatrix();

// we don't need to declare them anymore - bcs the caller is defined after their definitions:
//bool testMatrixArithmetic(std::string &reportString);
//bool testMatrixView();
//bool testMatrix2(); // test the new matrix class

#endif
